/// Canonical entry point for financial numeric input / formatting.
///
/// Prefer importing this barrel (or [app_amount_field.dart]) instead of
/// inventing per-module formatters.
library;

export '../../utils/grouped_decimal_input.dart';
export '../../widgets/app_amount_field.dart';
