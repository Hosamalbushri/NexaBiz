[project]
name = "stock-count-sync-backend"
version = "0.1.0"
description = "Experimental sync API for the Flutter ERP"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
